name: Deploy (prod)

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      spring-profile: prod
      image-tag: ${{ github.sha }}
    secrets: inherit
