name: Deploy (dev)

on:
  push:
    branches: [develop]

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      spring-profile: dev
      image-tag: dev-${{ github.sha }}
    secrets: inherit
