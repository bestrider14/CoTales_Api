name: Deploy to Dev

on:
  push:
    branches: [develop]

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: dev
      spring-profile: dev
      image-tag: dev-${{ github.sha }}
    secrets: inherit
