# Local state (migrate to S3 backend for team use)
*.tfstate
*.tfstate.backup

# Local config with secrets — copy from terraform.tfvars.example
terraform.tfvars

# Terraform working directory
.terraform/

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
